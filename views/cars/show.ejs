<%- include('../partials/html-head') %>
<link rel="stylesheet" href="/stylesheets/cars/show.css" />
<%- include('../partials/nav') %>

<main>
  <h1><%= title %></h1>
  <section>
    <div>Bought For: </div>
    <div><%= car.boughtFor %></div>
    <div>Year: </div>
    <div><%= car.year %></div>
    <div>Make: </div>
    <div><%= car.make %></div>
    <div>Model: </div>
    <div><%= car.model %></div>
    <% if (car.owner.equals(user?.profile._id)) { %>
      <form action="/cars/<%= car._id %>/edit" method="GET">
        <button id="edit-btn" class="btn" type="submit">Edit Car</button>
      </form>
    <% } %>
  </section>
    </section>
</main>
  
<%- include('../partials/footer') %>